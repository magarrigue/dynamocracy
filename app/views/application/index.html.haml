- if user_signed_in?  
  %h2= link_to "Your crews", crews_path if can? :read, Crew if can? :read, Crew
  - unless @has_crew
    %h2= link_to "Create a new crew", new_crew_path 
    .hint 
      You may create at most one crew.
    
  = render :partial => "news"
- else
  %br
  %br
  = link_to "Register now!", new_user_registration_path 
  %br
  %br
  or
  = link_to "Login", new_user_session_path
